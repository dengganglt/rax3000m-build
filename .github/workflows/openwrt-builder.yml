#
# https://github.com/P3TERX/Actions-OpenWrt
# 针对 RAX3000M 1GB 硬改版优化
#

name: OpenWrt Builder

on:
  repository_dispatch:
    types: [Source Code Update]
  workflow_dispatch:

env:
  # 解决 2026 年 Node 24 环境兼容性
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  REPO_URL: https://github.com/immortalwrt/immortalwrt
  REPO_BRANCH: openwrt-25.12
  CONFIG_FILE: .config
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-part2.sh
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    # [核心修复 1] 彻底扩容：解决 25.12 分支磁盘空间不足导致的 Exit 128
    - name: Maximize Build Space
      uses: easimon/maximize-build-space@master
      with:
        root-reserve-mb: 2048
        temp-reserve-mb: 1024
        remove-dotnet: 'true'
        remove-android: 'true'
        remove-haskell: 'true'
        remove-codeql: 'true'
        remove-docker-images: 'true'

    - name: Initialization Environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
        sudo timedatectl set-timezone "$TZ"
        sudo mkdir -p /workdir
        sudo chown $USER:$GROUPS /workdir

    - name: Clone Source Code
      working-directory: /workdir
      run: |
        df -hT $PWD
        git clone --depth 1 $REPO_URL -b $REPO_BRANCH openwrt
        ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

    - name: Load Custom Feeds
      run: |
        # [核心修复 2] 不再使用 chmod，直接用 bash 调用根目录脚本，解决 No such file 报错
        [ -f "$DIY_P1_SH" ] && bash "$DIY_P1_SH"
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: Load Custom Configuration
      run: |
        [ -e "$CONFIG_FILE" ] && cp "$CONFIG_FILE" openwrt/.config
        # [核心修复 3] 先进入目录，再运行脚本，确保脚本内的 sed 能找到 package 文件夹
        cd openwrt
        [ -f "../$DIY_P2_SH" ] && bash "../$DIY_P2_SH"

    - name: Compile the Firmware
      id: compile
      run: |
        cd openwrt
        make defconfig
        echo "::group::Download packages"
        make download -j8
        echo "::endgroup::"
        echo "编译线程数: $(nproc)"
        make -j$(nproc) || make -j1 V=s || { echo "Compile failed"; exit 1; }
        echo "status=success" >> $GITHUB_OUTPUT
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

    - name: Organize Files
      id: organize
      if: steps.compile.outputs.status == 'success' && !cancelled()
      run: |
        # [核心修复 4] 精准锁定 RAX3000M 固件生成目录，杜绝 /* 路径
        cd openwrt/bin/targets/mediatek/filogic/
        rm -rf packages .config* *.json *.buildinfo *.manifest *.sha256sums
        # 将当前 PWD（绝对路径）存入环境变量
        echo "FIRMWARE_DIR=$PWD" >> $GITHUB_ENV
        # 顺便创建发布日志，防止发布步骤因找不到 body 文件报错
        echo "RAX3000M 硬改 1GB 内存专用固件" > $GITHUB_WORKSPACE/release.txt
        echo "源码分支: ${{ env.REPO_BRANCH }}" >> $GITHUB_WORKSPACE/release.txt
        echo "编译时间: $(date +'%Y-%m-%d %H:%M')" >> $GITHUB_WORKSPACE/release.txt
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Upload Firmware to Release
      uses: softprops/action-gh-release@v2
      if: steps.organize.outputs.status == 'success' && !cancelled()
      with:
        tag_name: RAX3000M-1GB-${{ env.FILE_DATE }}
        body_path: release.txt
        # [核心修复 5] 绝对不要写 /*，锁定使用 organize 步骤生成的物理路径
        files: ${{ env.FIRMWARE_DIR }}/*
        token: ${{ secrets.GITHUB_TOKEN }}
        make_latest: true

    - name: Delete Old Workflow Runs
      uses: Mattraks/delete-workflow-runs@v2
      with:
        retain_days: 1
        keep_minimum_runs: 3
